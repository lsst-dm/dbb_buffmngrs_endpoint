database:
  engine: "postgres://tester:password@localhost:5432/test"
  orms:
    file: files
    status: gen2_statuses
    attempt: gen2_attempts
#  echo: false
ingester:
  plugin:
    name: Gen2Ingest
    config:
      root: /data/gen2repo
# file_status: untried
# batch_size: 10
# daemon: true
# pool_size: 1
# pause: 1
#logging:
#  file: null
#  format: "%(asctime)s:%(name)s:%(levelname)s:%(message)s"
#  level: WARNING
